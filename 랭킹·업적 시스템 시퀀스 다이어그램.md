# 랭킹·업적 시스템 시퀀스 다이어그램

---

## **랭킹 시스템 흐름**

### 1) 스테이지 클리어 + 랭킹 업데이트 흐름
**게임플레이 → 기록 갱신 → 서버 전송 → 랭킹 업데이트 → 캐시 갱신**

```mermaid
sequenceDiagram
    participant Player as 🎮 Player
    participant StageSystem as 🎯 StageManager
    participant RankingMgr as 🏆 RankingManager
    participant Steam as 🚂 SteamAuthManager
    participant Server as 🌐 ASP.NET Server
    participant RankingAPI as 📊 RankingController
    participant RankingService as ⚙️ RankingService
    participant Database as 🗄️ SQL Database
    participant Cache as 🚀 Redis Cache

    Note over Player, Cache: 1단계: 스테이지 클리어 및 기록 갱신
    Player->>StageSystem: 스테이지 클리어 (gems: 3/3, time: 25.5s, deaths: 2)
    activate StageSystem
    
    StageSystem->>StageSystem: 기존 최고 기록과 비교<br/>(이전: 30.2s, 5deaths)
    StageSystem->>StageSystem: ✅ 신기록 달성<br/>(clearTime: 25.5s ↓, deathCount: 2 ↓)
    
    StageSystem->>RankingMgr: OnBestTimeUpdated(stageNum: 1, clearTime: 25.5s)
    activate RankingMgr
    deactivate StageSystem

    Note over Player, Cache: 2단계: Steam 닉네임 조회 및 서버 전송
    RankingMgr->>Steam: GetSteamDisplayName()
    activate Steam
    Steam-->>RankingMgr: "JustClimber_2024"
    deactivate Steam
    
    RankingMgr->>RankingMgr: 📋 기록 업데이트 요청 생성<br/>{ stage: 1, time: 25.5s, deaths: 2, name: "JustClimber_2024" }
    
    RankingMgr->>Server: POST /api/ranking/{userId}/record<br/>JWT Authorization Header
    activate Server
    
    Server->>RankingAPI: UpdateUserRecord(userId, requestDto)
    activate RankingAPI
    
    RankingAPI->>RankingAPI: ✅ 입력 검증 통과<br/>(stageNum: 1~10, clearTime > 0, etc.)
    RankingAPI->>RankingService: ProcessRecordUpdate(userId, stage, time, deaths, displayName)
    activate RankingService
    
    RankingService->>Database: BEGIN TRANSACTION
    activate Database
    RankingService->>Database: UPSERT UserStageRecord<br/>SET clear_time=25.5, death_count=2, display_name='JustClimber_2024'
    RankingService->>Database: UPDATE last_updated = NOW()
    RankingService->>Database: COMMIT TRANSACTION
    Database-->>RankingService: ✅ 기록 업데이트 성공
    deactivate Database
    
    RankingService->>Cache: ZADD stage_1_time_ranking JustClimber_2024 25.5
    activate Cache
    RankingService->>Cache: ZADD stage_1_death_ranking JustClimber_2024 2
    Cache-->>RankingService: 랭킹 캐시 업데이트 완료
    deactivate Cache
    
    RankingService-->>RankingAPI: 처리 완료
    deactivate RankingService
    RankingAPI-->>Server: HTTP 200 OK
    deactivate RankingAPI
    Server-->>RankingMgr: 기록 업데이트 성공
    deactivate Server
    
    RankingMgr->>RankingMgr: 🧹 해당 스테이지 캐시 무효화<br/>(다음 조회 시 최신 데이터 로드)
    deactivate RankingMgr
```

### 2) 랭킹 조회 및 캐시 관리 흐름
**UI 요청 → 캐시 확인 → 서버 조회 → Top N + 내 기록 분리 → UI 표시**

```mermaid
sequenceDiagram
    participant UI as 🖥️ UI_Ranking
    participant RankingMgr as 🏆 RankingManager
    participant Server as 🌐 ASP.NET Server
    participant RankingAPI as 📊 RankingController
    participant RankingService as ⚙️ RankingService
    participant Cache as 🚀 Redis Cache
    participant Database as 🗄️ SQL Database

    Note over UI, Database: 랭킹 UI에서 데이터 요청
    UI->>RankingMgr: GetRankingWithMyEntry(stage: 1, sortType: ClearTime, maxTop: 20)
    activate RankingMgr
    
    RankingMgr->>RankingMgr: 🔍 로컬 캐시 확인<br/>_cachedRankings[1][ClearTime]
    
    alt 캐시에 데이터 있음
        RankingMgr-->>UI: ✅ 캐시된 데이터 즉시 반환<br/>(TopEntries: 20개, MyEntry: 15위)
    else 캐시 없음 - 서버 조회
        RankingMgr->>Server: GET /api/ranking?stageNumber=1&sortType=0&page=1&pageSize=20&userId={userId}
        activate Server
        
        Server->>RankingAPI: GetRanking(request, userId)
        activate RankingAPI
        
        RankingAPI->>RankingService: GetRankingAsync(userId, request)
        activate RankingService
        
        RankingService->>Cache: ZREVRANGE stage_1_time_ranking 0 19 WITHSCORES
        activate Cache
        Cache-->>RankingService: 📊 Top 20 랭킹 데이터
        deactivate Cache
        
        alt Redis 캐시에 데이터 있음
            RankingService->>RankingService: ✅ 캐시 데이터 활용
        else 캐시 없음 - DB 조회
            RankingService->>Database: SELECT TOP 20 FROM UserStageRecord<br/>WHERE stage_number=1 ORDER BY clear_time ASC
            activate Database
            Database-->>RankingService: Top 랭킹 데이터
            deactivate Database
            
            RankingService->>Cache: ZADD stage_1_time_ranking (캐시 재구성)
            activate Cache
            Cache-->>RankingService: 캐시 업데이트 완료
            deactivate Cache
        end
        
        RankingService->>Database: SELECT * FROM UserStageRecord<br/>WHERE user_id='{userId}' AND stage_number=1
        activate Database
        Database-->>RankingService: 내 기록 데이터
        deactivate Database
        
        RankingService->>RankingService: 📋 응답 데이터 구성<br/>{ TopEntries: [...], MyEntry: {rank: 15, time: 25.5s} }
        RankingService-->>RankingAPI: RankingResponseDto
        deactivate RankingService
        
        RankingAPI-->>Server: HTTP 200 + JSON 응답
        deactivate RankingAPI
        Server-->>RankingMgr: 랭킹 데이터 수신
        deactivate Server
        
        RankingMgr->>RankingMgr: 💾 로컬 캐시에 저장<br/>_cachedRankings[1][ClearTime] = response
        RankingMgr-->>UI: 🏆 Top 20 + 내 순위 반환<br/>OnRankingUpdated 이벤트 발생
    end
    deactivate RankingMgr
    
    UI->>UI: 🖥️ 랭킹 테이블 UI 업데이트<br/>(Top 20 리스트 + 내 기록 하이라이트)
```

---

## **업적 시스템 흐름**

### 3) 업적 달성 감지 및 해제 흐름
**게임 이벤트 → AchievementIntegration → 조건 체크 → Steam API 호출 → 서버 동기화 → 알림 표시**

```mermaid
sequenceDiagram
    participant Player as 🎮 Player
    participant StageSystem as 🎯 StageManager
    participant Integration as 🔗 AchievementIntegration
    participant AchievementMgr as 🏅 AchievementManager
    participant SteamAPI as 🚂 Steam API
    participant DataMgr as 💾 DataManager
    participant Server as 🌐 ASP.NET Server
    participant AchievementAPI as 🏆 AchievementController
    participant AchievementService as ⚙️ AchievementService
    participant Database as 🗄️ SQL Database

    Note over Player, Database: 1단계: 게임 이벤트 발생 (스테이지 클리어)
    Player->>StageSystem: 스테이지 1 클리어 (무사망, 모든 젬 수집)
    activate StageSystem
    
    StageSystem->>Integration: AchievementIntegration.OnStageCleared(stage: 1, time: 25.5s, deaths: 0, gems: 3, total: 3)
    activate Integration
    deactivate StageSystem
    
    Integration->>AchievementMgr: _achievementManager.OnStageCleared(stage: 1, time: 25.5s, deaths: 0, gems: 3, total: 3)
    activate AchievementMgr
    deactivate Integration
    
    AchievementMgr->>AchievementMgr: 📋 업적 조건 체크<br/>• 첫 스테이지 클리어 ✅<br/>• 완벽주의자 (무사망) ✅<br/>• 완벽한 수집가 (모든 젬) ✅

    Note over Player, Database: 2단계: Steam API를 통한 업적 해제
    loop 달성된 업적들 처리
        AchievementMgr->>SteamAPI: SetAchievement("NOVICE_CLIMBER")
        activate SteamAPI
        SteamAPI-->>AchievementMgr: ✅ Achievement Set Success
        
        AchievementMgr->>SteamAPI: StoreStats()
        SteamAPI-->>AchievementMgr: ✅ Steam 서버에 저장 완료
        deactivate SteamAPI
        
        AchievementMgr->>AchievementMgr: 💾 클라이언트 캐시 업데이트<br/>achievementUnlocked["NOVICE_CLIMBER"] = true
        AchievementMgr->>AchievementMgr: 🔔 업적 달성 알림 표시<br/>"🏆 업적 달성: 초보 등반가"
    end

    Note over Player, Database: 3단계: 서버 동기화 (백그라운드)
    AchievementMgr->>DataMgr: GenerateDelta("achievementUnlocked_NOVICE_CLIMBER", true)
    activate DataMgr
    DataMgr->>DataMgr: 📝 Delta 이벤트 큐에 추가<br/>(5초 후 배치 처리)
    deactivate DataMgr
    
    Note over Player, Database: (5초 후 배치 동기화)
    DataMgr->>Server: POST /api/users/{userId}/state/delta<br/>{ "deltas": [{"key": "achievementUnlocked_NOVICE_CLIMBER", "value": true}] }
    activate Server
    
    Server->>AchievementAPI: MergeDeltasAsync(userId, deltas)
    activate AchievementAPI
    
    AchievementAPI->>AchievementService: UpdateAchievementProgress(userId, achievementData)
    activate AchievementService
    
    AchievementService->>Database: UPDATE UserAchievement<br/>SET novice_climber = true WHERE user_id = '{userId}'
    activate Database
    Database-->>AchievementService: ✅ 업적 상태 업데이트 완료
    deactivate Database
    
    AchievementService-->>AchievementAPI: 동기화 성공
    deactivate AchievementService
    AchievementAPI-->>Server: HTTP 200 OK
    deactivate AchievementAPI
    Server-->>DataMgr: 서버 동기화 완료
    deactivate Server
    
    deactivate AchievementMgr
```

### 4) 업적 보상 수령 흐름
**UI에서 보상 요청 → 서버 검증 → 재화 지급 → 상태 업데이트**

```mermaid
sequenceDiagram
    participant Player as 🎮 Player
    participant UI as 🖥️ UI_Achievement
    participant AchievementMgr as 🏅 AchievementManager
    participant CurrencyMgr as 💰 CurrencyManager
    participant DataMgr as 💾 DataManager
    participant Server as 🌐 ASP.NET Server
    participant AchievementAPI as 🏆 AchievementController
    participant AchievementService as ⚙️ AchievementService
    participant Database as 🗄️ SQL Database

    Note over Player, Database: 1단계: 플레이어가 보상 수령 요청
    Player->>UI: "보상 받기" 버튼 클릭 (NOVICE_CLIMBER)
    activate UI
    
    UI->>AchievementMgr: ClaimReward("NOVICE_CLIMBER")
    activate AchievementMgr
    
    AchievementMgr->>AchievementMgr: ✅ 조건 검증<br/>• 업적 달성 여부: true<br/>• 보상 수령 여부: false
    
    Note over Player, Database: 2단계: 서버에 보상 수령 요청
    AchievementMgr->>Server: POST /api/achievement/NOVICE_CLIMBER/claim-reward<br/>JWT Authorization Header
    activate Server
    
    Server->>AchievementAPI: ClaimReward("NOVICE_CLIMBER")
    activate AchievementAPI
    
    AchievementAPI->>AchievementAPI: 🔐 JWT 토큰에서 사용자 ID 추출
    AchievementAPI->>AchievementService: ClaimRewardAsync(userId, "NOVICE_CLIMBER")
    activate AchievementService
    
    AchievementService->>Database: SELECT * FROM UserAchievement<br/>WHERE user_id='{userId}' AND achievement_id='NOVICE_CLIMBER'
    activate Database
    Database-->>AchievementService: 📊 업적 상태 조회 결과<br/>{ unlocked: true, reward_claimed: false }
    deactivate Database
    
    alt 보상 수령 조건 충족
        AchievementService->>Database: BEGIN TRANSACTION
        activate Database
        
        AchievementService->>Database: UPDATE UserAchievement<br/>SET reward_claimed = true, claimed_at = NOW()
        AchievementService->>Database: UPDATE Users<br/>SET gold = gold + 100, gems = gems + 5
        
        AchievementService->>Database: INSERT INTO UserTransactionLog<br/>(user_id, type, amount, reason)
        AchievementService->>Database: COMMIT TRANSACTION
        Database-->>AchievementService: ✅ 보상 지급 완료
        deactivate Database
        
        AchievementService-->>AchievementAPI: { success: true, rewards: { gold: 100, gems: 5 } }
        AchievementAPI-->>Server: HTTP 200 OK + 보상 정보
        Server-->>AchievementMgr: 보상 수령 성공 응답
        
        Note over Player, Database: 3단계: 클라이언트 상태 업데이트
        AchievementMgr->>AchievementMgr: 💾 로컬 보상 상태 업데이트<br/>achievementRewards["NOVICE_CLIMBER"] = true
        
        AchievementMgr->>CurrencyMgr: AddGold(100)
        activate CurrencyMgr
        AchievementMgr->>CurrencyMgr: AddGems(5)
        CurrencyMgr->>DataMgr: SaveData 업데이트 + Delta 생성
        activate DataMgr
        DataMgr-->>CurrencyMgr: 재화 변경 이벤트 발생
        deactivate DataMgr
        deactivate CurrencyMgr
        
        AchievementMgr->>DataMgr: GenerateDelta("achievementRewards", rewardData)
        activate DataMgr
        DataMgr-->>AchievementMgr: Delta 이벤트 생성 완료
        deactivate DataMgr
        
        AchievementMgr-->>UI: ✅ 보상 수령 완료<br/>{ gold: +100, gems: +5 }
        UI->>UI: 🎉 보상 수령 UI 업데이트<br/>• 버튼 → "수령 완료" (비활성화)<br/>• 골드/젬 카운터 증가 애니메이션
        
    else 보상 수령 불가 (이미 수령함 또는 조건 미달)
        AchievementService-->>AchievementAPI: { success: false, error: "Already claimed or not unlocked" }
        AchievementAPI-->>Server: HTTP 400 Bad Request
        Server-->>AchievementMgr: 보상 수령 실패
        AchievementMgr-->>UI: ❌ 오류: "이미 수령했거나 달성하지 않은 업적입니다"
    end
    
    deactivate AchievementService
    deactivate AchievementAPI
    deactivate Server
    deactivate AchievementMgr
    deactivate UI
```

### 5) 업적 진행도 실시간 추적 흐름
**아이템 사용 → AchievementIntegration → 진행도 누적 → 조건 달성 체크 → 자동 업적 해제**

```mermaid
sequenceDiagram
    participant Player as 🎮 Player
    participant ItemMgr as 📦 ItemManager
    participant Integration as 🔗 AchievementIntegration
    participant AchievementMgr as 🏅 AchievementManager
    participant DataMgr as 💾 DataManager
    participant SteamAPI as 🚂 Steam API

    Note over Player, SteamAPI: 아이템 사용을 통한 업적 진행도 추적
    Player->>ItemMgr: UseItem(Feather) - 10번째 사용
    activate ItemMgr
    
    ItemMgr->>Integration: AchievementIntegration.OnItemUsed("Feather")
    activate Integration
    
    Integration->>AchievementMgr: _achievementManager.OnItemUsed("Feather")
    activate AchievementMgr
    deactivate Integration
    
    AchievementMgr->>AchievementMgr: 📈 진행도 업데이트<br/>• usedItemInCurrentStage = true<br/>• itemTypesUsed.Add("Feather")<br/>• 사용한 아이템 종류: 4/10
    
    AchievementMgr->>AchievementMgr: 🔍 업적 조건 체크<br/>CheckItemUsageAchievements()
    
    alt 조건 달성 (모든 아이템 종류 사용)
        AchievementMgr->>AchievementMgr: ✅ "도구 마스터" 업적 조건 달성<br/>(itemTypesUsed.Count >= 10)
        
        AchievementMgr->>SteamAPI: SetAchievement("TOOL_MASTER")
        activate SteamAPI
        SteamAPI-->>AchievementMgr: Steam 업적 해제 완료
        deactivate SteamAPI
        
        AchievementMgr->>AchievementMgr: 🔔 "🏆 도구 마스터 업적 달성!" 알림
        
    else 조건 미달성
        AchievementMgr->>AchievementMgr: 📊 진행도만 업데이트<br/>(4/10 아이템 종류 사용)
    end
    
    AchievementMgr->>DataMgr: GenerateDelta("achievementProgress", progressData)
    activate DataMgr
    DataMgr-->>AchievementMgr: 서버 동기화 큐에 추가
    deactivate DataMgr
    
    deactivate AchievementMgr
    deactivate ItemMgr
```

---

## **시스템 특징 및 아키텍처**

### **랭킹 시스템 특징**
- **실시간 기록 업데이트**: `StageManager.OnBestTimeUpdated` 이벤트 기반 자동 서버 전송
- **이중 캐시 구조**: Redis (서버) + Dictionary (클라이언트) 계층 캐싱으로 성능 최적화
- **Top N + 내 기록 분리**: 상위 랭킹과 개인 기록을 별도 조회하여 UI 최적화
- **중복 요청 방지**: `_pendingUpdates` HashSet으로 동일 스테이지 연속 요청 차단
- **Steam 닉네임 연동**: Steam 프로필 정보 자동 동기화 및 표시

### **업적 시스템 특징**  
- **Facade Pattern**: `AchievementIntegration` 정적 클래스가 복잡한 `AchievementManager`에 대한 단순한 인터페이스 제공
- **느슨한 결합**: 다른 매니저들이 `AchievementManager`에 직접 의존하지 않고 `AchievementIntegration`을 통해 통신
- **통합된 이벤트 채널**: 모든 게임 이벤트가 `AchievementIntegration`을 통해 일관된 방식으로 처리
  - `GameManager` → `OnStageStart()`, `OnPlayerDeath()`
  - `StageManager` → `OnStageCleared()`
  - `ItemManager` → `OnItemUsed()`, `OnItemPurchased()`
- **이중 플랫폼 동기화**: Steam API + 서버 DB 동시 업적 해제로 데이터 무결성 보장
- **실시간 조건 체크**: 게임 이벤트 발생 즉시 업적 조건 검증 및 해제
- **진행도 추적**: 누적형 업적을 위한 상세 진행 상태 관리 (`AchievementProgress`)
- **보상 시스템**: 업적 달성 시 게임 내 재화(골드/젬) 자동 지급
- **오프라인 지원**: Steam API 실패 시에도 서버 DB 동기화로 데이터 손실 방지
- **Delta 동기화**: 배치 처리를 통한 업적 상태 서버 전송으로 네트워크 효율성 최적화 