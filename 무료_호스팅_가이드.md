# Just Climb! 무료 호스팅 가이드 💰

## 🆓 완전 무료 옵션 (개발/테스트용)

### 1. **AWS Free Tier** ⭐⭐⭐⭐⭐
#### 📊 무료 제공 내용 (12개월)
- **EC2**: t2.micro 인스턴스 750시간/월
- **RDS**: db.t2.micro 750시간/월 + 20GB 스토리지
- **ElastiCache**: 없음 (Redis 대신 메모리 캐시 사용)
- **Application Load Balancer**: 750시간/월
- **Certificate Manager**: 무료 SSL 인증서

#### 💡 Just Climb 적용 방법
```bash
# 무료 티어로 구성
- 서버: EC2 t2.micro (1 vCPU, 1GB RAM)
- DB: RDS PostgreSQL db.t2.micro (1 vCPU, 1GB RAM)
- 캐시: 인메모리 캐시 (Redis 대신)
- SSL: Certificate Manager 무료 인증서
```

#### ⚠️ 제한사항
- 성능이 제한적 (동시 접속자 10-50명 정도)
- 12개월 후 요금 발생
- Redis 캐시 없음 (성능 영향)

---

### 2. **Railway** ⭐⭐⭐⭐⭐
#### 📊 무료 플랜
- **서버**: 512MB RAM, 1 vCPU
- **데이터베이스**: PostgreSQL 1GB
- **무료 사용량**: $5 크레딧/월
- **SSL**: 자동 제공
- **도메인**: 자동 할당

#### 💡 설정 방법
```bash
# 1. Railway 계정 생성 (GitHub으로 간단 가입)
npm install -g @railway/cli
railway login

# 2. 프로젝트 생성
railway init

# 3. 데이터베이스 추가
railway add postgresql

# 4. 환경 변수 설정 (자동)
# 5. 배포
railway up

# 자동으로 HTTPS 도메인 할당: your-app.railway.app
```

#### ✅ 장점
- 설정 매우 간단 (5분 완료)
- 자동 SSL 및 도메인
- GitHub 연동 자동 배포
- PostgreSQL 포함

#### ⚠️ 제한사항
- $5 크레딧 초과 시 요금 발생 (~1000 요청/일 정도)
- Redis 별도 추가 필요

---

### 3. **Supabase + Vercel** ⭐⭐⭐⭐
#### 📊 무료 제공
- **Supabase**: PostgreSQL + 인증 + API
- **Vercel**: 서버리스 함수 호스팅
- **SSL**: 자동 제공

#### 💡 설정 방법
```bash
# 1. Supabase 프로젝트 생성
https://supabase.com → 새 프로젝트 생성

# 2. 데이터베이스 스키마 생성
-- Supabase SQL 에디터에서 테이블 생성

# 3. Vercel에 API 배포
npm install -g vercel
vercel

# 4. 환경 변수 설정
vercel env add SUPABASE_URL
vercel env add SUPABASE_ANON_KEY
```

#### ✅ 장점
- 완전 무료 (상당한 사용량까지)
- 실시간 데이터베이스
- 자동 API 생성
- 서버리스 확장

#### ⚠️ 제한사항
- .NET Core → Node.js/Python 변경 필요
- 게임 서버로는 제한적

---

## 💵 저비용 옵션 ($5-15/월)

### 1. **DigitalOcean** ⭐⭐⭐⭐
#### 📊 최소 요금제
- **Droplet**: $5/월 (1GB RAM, 1 vCPU)
- **Managed Database**: $15/월 (PostgreSQL)
- **SSL**: Let's Encrypt 무료

#### 💡 설정 방법
```bash
# 1. Droplet 생성 (Ubuntu 22.04)
doctl compute droplet create justclimb \
  --image ubuntu-22-04-x64 \
  --size s-1vcpu-1gb \
  --region nyc3

# 2. Docker 설치 및 앱 배포
ssh root@your-droplet-ip
apt update && apt install docker.io docker-compose
git clone your-repo
docker-compose up -d

# 3. 도메인 연결 및 SSL 설정
certbot --nginx -d api.justclimb.com
```

#### ✅ 장점
- 예측 가능한 요금
- 완전한 서버 제어
- 성능 좋음

---

### 2. **PlanetScale + Railway** ⭐⭐⭐⭐
#### 📊 무료/저비용 조합
- **PlanetScale**: MySQL 무료 5GB
- **Railway**: 서버 호스팅 $5/월
- **Redis**: Railway Redis $3/월

#### 💡 장점
- PlanetScale 무료 DB (브랜치 기능)
- Railway 간단한 배포
- 총 비용 $8/월

---

## 🎯 Just Climb 추천 무료 시작 방법

### 🥇 **1순위: Railway** (가장 간단)
```bash
# 30초 배포
railway login
railway init
railway add postgresql redis
railway up

# 결과: https://your-app.railway.app
# 비용: 무료 ($5 크레딧/월)
```

### 🥈 **2순위: AWS Free Tier** (학습 목적)
```bash
# 완전한 클라우드 경험
# 12개월 무료
# 이후 AWS 생태계 확장 가능
```

### 🥉 **3순위: DigitalOcean** (안정성)
```bash
# $20/월 예측 가능
# 높은 성능
# 간단한 관리
```

---

## 📊 비용 비교표

| **서비스** | **서버** | **DB** | **Redis** | **SSL** | **총 월비용** |
|------------|----------|--------|-----------|---------|---------------|
| **Railway** | 무료 | 무료 | $3 | 무료 | **무료~$3** |
| **AWS Free** | 무료 | 무료 | 없음 | 무료 | **무료** |
| **Supabase+Vercel** | 무료 | 무료 | 없음 | 무료 | **무료** |
| **DigitalOcean** | $5 | $15 | $5 | 무료 | **$25** |
| **AWS 유료** | $10 | $15 | $15 | 무료 | **$40** |

---

## 🚀 Railway 무료 시작하기 (5분)

### 1. 계정 생성 및 프로젝트 설정
```bash
# 1. Railway 웹사이트에서 GitHub으로 가입
https://railway.app

# 2. CLI 설치
npm install -g @railway/cli

# 3. 로그인
railway login
```

### 2. 프로젝트 배포
```bash
# 1. 프로젝트 폴더에서
cd /c/Users/user/Just_Climb

# 2. Railway 프로젝트 초기화
railway init

# 3. 서비스 추가
railway add postgresql  # PostgreSQL 데이터베이스
railway add redis       # Redis 캐시 (선택사항)

# 4. 환경 변수 자동 설정됨
railway variables

# 5. 서버 배포
cd Server
railway up
```

### 3. Unity 클라이언트 설정
```csharp
// Assets/Scripts/Config/ServerConfig.cs 업데이트
#if UNITY_EDITOR
    public const string BASE_URL = "https://localhost:5001";
#else
    public const string BASE_URL = "https://your-app.railway.app"; // Railway 도메인
#endif
```

### 4. 테스트
```bash
# API 테스트
curl https://your-app.railway.app/api/v1/health

# Unity에서 연결 테스트
# 게임 실행 → 서버 통신 확인
```

---

## 💡 무료 사용 팁

### 1. **트래픽 최적화**
```csharp
// API 호출 최소화
public class CacheManager
{
    private static Dictionary<string, object> _cache = new();
    private static DateTime _lastUpdate = DateTime.MinValue;
    
    public static async Task<T> GetCachedData<T>(string key, Func<Task<T>> fetchData, TimeSpan cacheTime)
    {
        if (_cache.ContainsKey(key) && DateTime.Now - _lastUpdate < cacheTime)
        {
            return (T)_cache[key];
        }
        
        var data = await fetchData();
        _cache[key] = data;
        _lastUpdate = DateTime.Now;
        return data;
    }
}
```

### 2. **데이터베이스 최적화**
```sql
-- 인덱스 추가로 쿼리 성능 향상
CREATE INDEX idx_user_steam_id ON Users(steam_id);
CREATE INDEX idx_ranking_stage_time ON Rankings(stage_id, best_time);
CREATE INDEX idx_achievement_user ON UserAchievements(user_id);
```

### 3. **모니터링**
```bash
# Railway 사용량 확인
railway logs

# 비용 모니터링
railway usage
```

---

## 🎉 완료!

### ✅ 추천 시작 방법
1. **지금 바로**: Railway 무료 시작
2. **나중에 업그레이드**: AWS나 DigitalOcean으로 이전
3. **Steam 출시 후**: 수익 발생 시 유료 플랜

### 🔄 업그레이드 경로
```
Railway 무료 → Railway $5/월 → DigitalOcean $25/월 → AWS $60/월
```

**Railway로 시작하면 5분 안에 무료로 Just Climb 서버가 실행됩니다!** 🚀

---

## 🔄 Railway 배포 워크플로우 완벽 가이드

### 🚀 1단계: 개발 환경 준비

#### 1.1 필수 도구 설치
```bash
# Node.js 설치 (Railway CLI 용)
# https://nodejs.org 에서 설치

# Railway CLI 설치
npm install -g @railway/cli

# Docker 설치 (선택사항)
# https://docs.docker.com/get-docker/

# Git 설정 확인
git --version
```

#### 1.2 프로젝트 구조 확인
```
Just_Climb/
├── Server/
│   ├── Server/
│   │   ├── Program.cs
│   │   ├── appsettings.Railway.json
│   │   └── ...
│   ├── Dockerfile
│   └── railway.toml
├── Assets/
│   └── Scripts/
│       └── Config/
│           └── ServerConfig.cs
└── .github/
    └── workflows/
        └── railway-deploy.yml
```

### 🎯 2단계: Railway 프로젝트 생성

#### 2.1 웹에서 프로젝트 생성
```bash
# 1. Railway 웹사이트 접속
https://railway.app

# 2. GitHub 계정으로 로그인
# 3. "New Project" → "Deploy from GitHub repo"
# 4. Just_Climb 저장소 선택
# 5. "Deploy Now" 클릭
```

#### 2.2 CLI로 프로젝트 연결
```bash
# 프로젝트 폴더로 이동
cd /c/Users/user/Just_Climb

# Railway CLI 로그인
railway login

# 기존 프로젝트에 연결
railway link

# 또는 새 프로젝트 생성
railway init
```

### 🗄️ 3단계: 데이터베이스 설정

#### 3.1 PostgreSQL 추가
```bash
# Railway 대시보드에서 또는 CLI로
railway add postgresql

# 환경 변수 자동 생성 확인
railway variables

# 출력 예시:
# DATABASE_URL=postgresql://user:pass@host:port/db
# PGDATABASE=railway
# PGHOST=containers-us-west-1.railway.app
# PGPASSWORD=your-password
# PGPORT=5432
# PGUSER=postgres
```

#### 3.2 Redis 추가 (선택사항)
```bash
# 캐시 성능 향상을 위해
railway add redis

# Redis 환경 변수 확인
# REDIS_URL=redis://default:password@host:port
```

### ⚙️ 4단계: 환경 변수 설정

#### 4.1 Railway 환경 변수 설정
```bash
# 웹 대시보드에서 Variables 탭 또는 CLI로
railway variables set ASPNETCORE_ENVIRONMENT=Production
railway variables set ASPNETCORE_URLS=http://0.0.0.0:$PORT
railway variables set CORS_ORIGINS=https://your-unity-client.com

# JWT 시크릿 키 설정
railway variables set JWT_SECRET_KEY=your-super-secure-secret-key-here

# Steam API 키 설정
railway variables set STEAM_API_KEY=your-steam-api-key
```

#### 4.2 appsettings.Railway.json 확인
```json
{
  "ConnectionStrings": {
    "DefaultConnection": "{DATABASE_URL}",
    "Redis": "{REDIS_URL}"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Cors": {
    "Origins": [
      "https://your-app.railway.app",
      "https://your-custom-domain.com"
    ]
  },
  "Jwt": {
    "Key": "{JWT_SECRET_KEY}",
    "Issuer": "JustClimb",
    "Audience": "JustClimbUsers",
    "ExpiryMinutes": 60
  },
  "Steam": {
    "ApiKey": "{STEAM_API_KEY}"
  }
}
```

### 🚀 5단계: 첫 배포

#### 5.1 수동 배포
```bash
# 서버 프로젝트 폴더로 이동
cd Server

# Railway에 배포
railway up

# 배포 로그 확인
railway logs

# 도메인 확인
railway domain
# 출력: https://your-app.railway.app
```

#### 5.2 배포 상태 확인
```bash
# 서비스 상태 확인
railway status

# 데이터베이스 마이그레이션 확인
railway run dotnet ef database update

# API 테스트
curl https://your-app.railway.app/api/v1/health
```

### 🔄 6단계: CI/CD 파이프라인 설정

#### 6.1 GitHub Actions 설정
```yaml
# .github/workflows/railway-deploy.yml
name: Deploy to Railway

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: 6.0.x
    
    - name: Install Railway CLI
      run: npm install -g @railway/cli
    
    - name: Deploy to Railway
      run: railway up --service ${{ secrets.RAILWAY_SERVICE_ID }}
      env:
        RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
      working-directory: ./Server
```

#### 6.2 GitHub Secrets 설정
```bash
# GitHub Repository → Settings → Secrets and variables → Actions

# 추가할 시크릿:
RAILWAY_TOKEN=your-railway-token
RAILWAY_SERVICE_ID=your-service-id

# Railway 토큰 얻기
railway login
railway whoami
```

### 🌐 7단계: 도메인 연결

#### 7.1 커스텀 도메인 설정
```bash
# Railway 대시보드에서
# Settings → Domains → Add Domain
# 예: api.justclimb.com

# DNS 설정 (도메인 업체에서)
# CNAME 레코드 추가:
# api.justclimb.com → your-app.railway.app
```

#### 7.2 Unity 클라이언트 업데이트
```csharp
// Assets/Scripts/Config/ServerConfig.cs
public static class ServerConfig
{
    #if UNITY_EDITOR
        public const string BASE_URL = "https://localhost:5001";
    #else
        public const string BASE_URL = "https://api.justclimb.com"; // 커스텀 도메인
    #endif
    
    public static string GetApiUrl(string endpoint)
    {
        return $"{BASE_URL}/api/v1{endpoint}";
    }
}
```

### 📊 8단계: 모니터링 및 로그 관리

#### 8.1 실시간 로그 확인
```bash
# 실시간 로그 스트리밍
railway logs --tail

# 특정 시간대 로그
railway logs --since=1h

# 에러 로그만 필터링
railway logs --tail | grep ERROR
```

#### 8.2 메트릭 확인
```bash
# 서비스 상태 확인
railway status

# 메모리 및 CPU 사용량 확인
railway metrics

# 데이터베이스 연결 상태
railway connect postgresql
```

### 🔧 9단계: 업데이트 및 롤백

#### 9.1 코드 업데이트 프로세스
```bash
# 1. 개발 브랜치에서 작업
git checkout develop
git add .
git commit -m "feat: 새로운 기능 추가"
git push origin develop

# 2. PR 생성 및 코드 리뷰
# 3. main 브랜치로 머지
git checkout main
git merge develop
git push origin main

# 4. 자동 배포 (GitHub Actions)
# 5. 배포 확인
railway logs --tail
```

#### 9.2 롤백 프로세스
```bash
# 이전 배포 버전으로 롤백
railway rollback

# 특정 Git 커밋으로 롤백
git checkout <commit-hash>
railway up

# 긴급 롤백 (웹 대시보드에서)
# Railway 대시보드 → Deployments → 이전 버전 → Redeploy
```

### 🚨 10단계: 문제 해결 가이드

#### 10.1 일반적인 문제들
```bash
# 1. 배포 실패
railway logs --tail
# 해결: 환경 변수 확인, Dockerfile 문법 체크

# 2. 데이터베이스 연결 실패
railway variables | grep DATABASE
# 해결: 연결 문자열 확인, 마이그레이션 실행

# 3. 메모리 부족
railway metrics
# 해결: 코드 최적화, 플랜 업그레이드

# 4. SSL 인증서 문제
curl -I https://your-app.railway.app
# 해결: 도메인 설정 확인, DNS 전파 대기
```

#### 10.2 디버깅 도구
```bash
# 컨테이너 내부 접속
railway shell

# 데이터베이스 직접 접속
railway connect postgresql

# 환경 변수 확인
railway run env

# 서비스 재시작
railway restart
```

### 📈 11단계: 성능 최적화

#### 11.1 무료 플랜 최적화
```csharp
// 캐시 사용으로 DB 호출 최소화
[Route("api/v1/[controller]")]
[ApiController]
public class RankingController : ControllerBase
{
    private readonly IMemoryCache _cache;
    
    [HttpGet]
    public async Task<IActionResult> GetRankings()
    {
        const string cacheKey = "rankings";
        if (_cache.TryGetValue(cacheKey, out var cachedRankings))
        {
            return Ok(cachedRankings);
        }
        
        var rankings = await _rankingService.GetRankingsAsync();
        _cache.Set(cacheKey, rankings, TimeSpan.FromMinutes(5));
        
        return Ok(rankings);
    }
}
```

#### 11.2 데이터베이스 최적화
```sql
-- 자주 사용하는 쿼리에 인덱스 추가
CREATE INDEX idx_user_steam_id ON Users(steam_id);
CREATE INDEX idx_ranking_stage_time ON Rankings(stage_id, best_time);
CREATE INDEX idx_achievement_user_created ON UserAchievements(user_id, created_at);
```

### 🎯 12단계: 운영 체크리스트

#### 12.1 배포 전 체크리스트
- [ ] 로컬 테스트 통과
- [ ] 환경 변수 설정 완료
- [ ] 데이터베이스 마이그레이션 준비
- [ ] SSL 인증서 유효성 확인
- [ ] 백업 계획 수립

#### 12.2 배포 후 체크리스트
- [ ] API 엔드포인트 응답 확인
- [ ] 데이터베이스 연결 상태 확인
- [ ] Unity 클라이언트 연결 테스트
- [ ] 로그 에러 메시지 확인
- [ ] 성능 메트릭 모니터링

### 💡 13단계: 비용 관리

#### 13.1 무료 크레딧 모니터링
```bash
# 현재 사용량 확인
railway usage

# 월별 사용량 추이
railway metrics --range=30d

# 알림 설정 (웹 대시보드에서)
# Settings → Usage → Set up alerts
```

#### 13.2 비용 절약 팁
- 불필요한 로그 레벨 제거
- 캐시 활용으로 DB 호출 최소화
- 이미지 최적화로 배포 속도 향상
- 슬립 모드 활용 (개발 환경)

---

## 🎉 워크플로우 완료!

### ✅ 일일 운영 루틴
1. **아침**: 로그 확인 및 에러 체크
2. **개발**: 브랜치 작업 → PR → 머지 → 자동 배포
3. **저녁**: 사용량 모니터링 및 성능 체크

### 🔄 주간 운영 루틴
1. **월요일**: 주간 계획 및 백업 확인
2. **수요일**: 성능 최적화 및 로그 분석
3. **금요일**: 업데이트 배포 및 주간 리뷰

### 📞 문제 발생 시 대응
1. Railway 대시보드에서 즉시 상태 확인
2. 로그 분석으로 원인 파악
3. 필요 시 이전 버전으로 롤백
4. 문제 해결 후 다시 배포

**이제 Railway로 안정적인 배포 워크플로우가 완성되었습니다!** 🚀

어떤 무료 옵션부터 시작해보실까요? 