## 아이템·재화 시스템 시퀀스 다이어그램

---

## **📱 클라이언트 내부 흐름 (Local Processing)**

### 1) 상점에서 아이템 구매 흐름
**Player → UI_Shop → CurrencyManager → ItemManager → DataManager → UI 갱신**

```mermaid
sequenceDiagram
    participant Player as 🎮 Player
    participant Shop as 🏪 UI_Shop
    participant Currency as 💰 CurrencyManager
    participant ItemMgr as 📦 ItemManager
    participant ItemDB as 📚 ItemDatabase
    participant DataMgr as 💾 DataManager
    participant Inventory as 🎒 UI_Inventory

    Player->>Shop: BuyItem(ItemType.Feather) 클릭
    activate Shop

    Shop->>Currency: SpendGold(price)
    activate Currency
    
    Currency->>Currency: 잔액 확인 & 차감
    Currency->>DataMgr: SaveData.gold -= price
    activate DataMgr
    
    DataMgr->>DataMgr: JSON 저장 + Delta 이벤트 발생
    DataMgr-->>Currency: OnGoldChanged(newGold)
    deactivate DataMgr
    deactivate Currency

    Shop->>ItemMgr: AddItem(ItemType.Feather, 1)
    activate ItemMgr
    
    ItemMgr->>ItemDB: GetItemData(ItemType.Feather)
    activate ItemDB
    ItemDB-->>ItemMgr: FeatherData.asset 반환
    deactivate ItemDB
    
    ItemMgr->>ItemMgr: SetItemCountInternal(Feather, +1)
    ItemMgr->>DataMgr: SaveInventory()
    activate DataMgr
    
    DataMgr->>DataMgr: JSON 저장 + Delta 이벤트 발생
    DataMgr-->>ItemMgr: OnInventoryChanged(itemType, newCount)
    deactivate DataMgr

    ItemMgr-->>Shop: OnItemCountChanged(Feather, newCount)
    ItemMgr-->>Inventory: OnItemCountChanged(Feather, newCount)
    deactivate ItemMgr

    Shop->>Shop: 💰 골드/젬 UI 업데이트
    Inventory->>Inventory: 📦 아이템 슬롯 UI 업데이트
    deactivate Shop
```

### 2) 인게임에서 아이템 사용 흐름
**키 입력 → 사용 가능 체크 → IItemUse 구현체 실행 → 버프 적용 → 데이터 저장 → UI 갱신**

```mermaid
sequenceDiagram
    participant Player as 🎮 Player
    participant Input as ⌨️ ItemInput
    participant ItemMgr as 📦 ItemManager
    participant ItemDB as 📚 ItemDatabase
    participant UseLogic as 🪶 FeatherUse
    participant Locomotion as 🏃 PlayerLocomotion
    participant DataMgr as 💾 DataManager
    participant Inventory as 🎒 UI_Inventory

    Player->>Input: 키 '1' (Feather) 입력
    activate Input

    Input->>ItemMgr: UseItem(ItemType.Feather, player)
    activate ItemMgr

    ItemMgr->>ItemMgr: 📋 쿨다운 & 보유량 체크
    alt 사용 가능
        ItemMgr->>ItemDB: GetItemData(ItemType.Feather)
        activate ItemDB
        ItemDB-->>ItemMgr: FeatherData.asset + IItemUse 구현체
        deactivate ItemDB
        
        ItemMgr->>UseLogic: Use(player)
        activate UseLogic
        UseLogic->>Locomotion: ApplyFeatherBuff(jumpHeight, duration)
        activate Locomotion
        Locomotion->>Locomotion: 🚀 점프력 증가 버프 적용
        deactivate Locomotion
        deactivate UseLogic

        ItemMgr->>ItemMgr: RemoveItem(Feather, 1)
        ItemMgr->>DataMgr: SaveInventory()
        activate DataMgr
        
        DataMgr->>DataMgr: JSON 저장 + Delta 이벤트 발생
        DataMgr-->>ItemMgr: OnInventoryChanged(Feather, newCount)
        deactivate DataMgr

        ItemMgr-->>Inventory: OnItemCountChanged(Feather, newCount)
        ItemMgr-->>Input: 사용 성공
    else 사용 불가 (쿨다운/보유량 부족)
        ItemMgr-->>Input: 사용 실패
    end
    deactivate ItemMgr

    Inventory->>Inventory: 🎒 개수/쿨타임/버프 UI 업데이트
    deactivate Input
```

---

## **🌐 클라이언트-서버 통신 흐름 (Network Sync)**

### 3) 아이템 구매 + 서버 동기화 흐름
**로컬 구매 → Delta 이벤트 수집 → 서버 배치 업로드 → 검증 및 동기화**

```mermaid
sequenceDiagram
    participant Player as 🎮 Player
    participant Shop as 🏪 UI_Shop
    participant Currency as 💰 CurrencyManager
    participant ItemMgr as 📦 ItemManager
    participant DataMgr as 💾 DataManager
    participant SyncMgr as 🔄 DataSyncManager
    participant Server as 🌐 ASP.NET Server
    participant SaveAPI as 💾 SaveController
    participant Database as 🗄️ SQL Database
    participant Cache as 🚀 Redis Cache

    Note over Player, Cache: 1단계: 로컬 즉시 처리 (반응성 보장)
    Player->>Shop: BuyItem(ItemType.Feather, 100G) 클릭
    Shop->>Currency: SpendGold(100)
    Currency->>DataMgr: Current.gold -= 100
    DataMgr->>DataMgr: 📝 Delta 이벤트 생성<br/>[gold: -100]
    Shop->>ItemMgr: AddItem(Feather, 1)
    ItemMgr->>DataMgr: Current.inventory[Feather] += 1
    DataMgr->>DataMgr: 📝 Delta 이벤트 생성<br/>[inventory.Feather: +1]
    DataMgr-->>Shop: UI 즉시 업데이트

    Note over Player, Cache: 2단계: 백그라운드 서버 동기화
    DataMgr->>SyncMgr: 5초 후 배치 처리 트리거
    activate SyncMgr
    
    SyncMgr->>SyncMgr: 📋 Delta 이벤트 수집<br/>[gold: -100, inventory.Feather: +1]
    SyncMgr->>Server: POST /api/users/{userId}/state/delta<br/>{ "deltas": [...] }
    activate Server
    
    Server->>SaveAPI: ProcessDeltaEvents(userId, deltas)
    activate SaveAPI
    
    SaveAPI->>Database: BEGIN TRANSACTION
    activate Database
    SaveAPI->>Database: UPDATE Users SET gold = gold - 100
    SaveAPI->>Database: UPDATE UserInventory SET feather_count = feather_count + 1
    SaveAPI->>Database: COMMIT TRANSACTION
    Database-->>SaveAPI: ✅ 성공
    deactivate Database
    
    SaveAPI->>Cache: InvalidateUserState(userId)
    activate Cache
    Cache-->>SaveAPI: 캐시 무효화 완료
    deactivate Cache
    
    SaveAPI-->>Server: Delta 처리 성공
    deactivate SaveAPI
    Server-->>SyncMgr: HTTP 200 OK
    deactivate Server
    
    SyncMgr->>DataMgr: OnServerSyncComplete()
    SyncMgr->>SyncMgr: 🧹 로컬 Delta 큐 정리
    deactivate SyncMgr
```

### 4) 아이템 사용 + 서버 로깅 흐름
**로컬 사용 → 서버 사용 로그 → 통계 업데이트 → 이상 감지 시 동기화**

```mermaid
sequenceDiagram
    participant Player as 🎮 Player
    participant ItemMgr as 📦 ItemManager
    participant DataMgr as 💾 DataManager
    participant SyncMgr as 🔄 DataSyncManager
    participant Server as 🌐 ASP.NET Server
    participant SaveAPI as 💾 SaveController
    participant RankingAPI as 🏆 RankingController
    participant Database as 🗄️ SQL Database

    Note over Player, Database: 1단계: 로컬 즉시 사용 (게임플레이 우선)
    Player->>ItemMgr: UseItem(Feather)
    ItemMgr->>ItemMgr: ✅ 쿨다운/개수 검증 통과
    ItemMgr->>ItemMgr: 🚀 버프 효과 적용
    ItemMgr->>DataMgr: RemoveItem(Feather, 1)
    DataMgr->>DataMgr: 📝 Delta 이벤트 생성<br/>[inventory.Feather: -1, stats.featherUsed: +1]

    Note over Player, Database: 2단계: 서버 사용 로그 및 검증
    DataMgr->>SyncMgr: 배치 동기화 (5초 간격)
    activate SyncMgr
    
    SyncMgr->>Server: POST /api/users/{userId}/state/delta<br/>{ "deltas": [inventory.Feather: -1, stats.featherUsed: +1] }
    activate Server
    
    Server->>SaveAPI: ProcessDeltaEvents(userId, deltas)
    activate SaveAPI
    
    SaveAPI->>Database: UPDATE UserInventory SET feather_count = feather_count - 1
    activate Database
    SaveAPI->>Database: UPDATE UserStats SET feather_used = feather_used + 1
    Database-->>SaveAPI: ✅ 업데이트 성공
    deactivate Database
    
    alt 서버 검증 성공
        SaveAPI-->>Server: Delta 처리 완료
        Server-->>SyncMgr: HTTP 200 OK
        SyncMgr->>DataMgr: 🧹 Delta 큐 정리
        
    else 이상 사용 패턴 감지
        SaveAPI->>SaveAPI: 🚨 비정상 사용 감지<br/>(과도한 사용 빈도)
        SaveAPI->>Database: SELECT current state FROM Users
        activate Database
        Database-->>SaveAPI: 서버 정확한 상태 반환
        deactivate Database
        
        SaveAPI-->>Server: HTTP 409 Conflict + 정확한 상태
        Server-->>SyncMgr: 동기화 충돌 응답
        SyncMgr->>DataMgr: ForceSync(serverState)
        DataMgr->>DataMgr: 🔄 로컬 상태를 서버 상태로 덮어쓰기
        DataMgr-->>ItemMgr: OnInventoryChanged(정정된 개수)
    end
    
    deactivate SaveAPI
    deactivate Server
    deactivate SyncMgr

    Note over Player, Database: 3단계: 통계 및 랭킹 업데이트 (선택적)
    alt 스테이지 클리어 상황
        Server->>RankingAPI: UpdateGameStats(userId, itemUsage)
        activate RankingAPI
        RankingAPI->>Database: UPDATE Leaderboards
        RankingAPI-->>Server: 랭킹 업데이트 완료
        deactivate RankingAPI
    end
```

### 5) 네트워크 단절 + 복구 시나리오
**오프라인 플레이 → 온라인 복구 → 충돌 해결 → 상태 동기화**

```mermaid
sequenceDiagram
    participant Player as 🎮 Player
    participant DataMgr as 💾 DataManager
    participant SyncMgr as 🔄 DataSyncManager
    participant CacheMgr as 📦 OfflineCacheManager
    participant Server as 🌐 ASP.NET Server
    participant SaveAPI as 💾 SaveController

    Note over Player, SaveAPI: 오프라인 상태에서 게임 플레이
    Player->>DataMgr: 아이템 구매/사용 (오프라인)
    DataMgr->>DataMgr: 📝 Delta 이벤트 누적<br/>[gold: -200, feather: +2, feather_used: +1]
    DataMgr->>CacheMgr: StoreOfflineDeltas(deltas)
    activate CacheMgr
    CacheMgr->>CacheMgr: 💾 로컬 파일 시스템에 Delta 저장
    DataMgr-->>Player: ✅ 오프라인 모드로 게임 지속

    Note over Player, SaveAPI: 네트워크 복구 감지
    SyncMgr->>SyncMgr: 🌐 네트워크 상태 확인 (주기적)
    SyncMgr->>Server: HEAD /api/users/{userId}/state (연결 테스트)
    activate Server
    Server-->>SyncMgr: HTTP 200 OK (연결 복구)
    deactivate Server

    SyncMgr->>CacheMgr: GetOfflineDeltas()
    CacheMgr-->>SyncMgr: 🗂️ 누적된 Delta 이벤트 반환
    deactivate CacheMgr

    Note over Player, SaveAPI: 충돌 해결 및 동기화
    SyncMgr->>Server: GET /api/users/{userId}/state (서버 최신 상태)
    activate Server
    Server->>SaveAPI: GetUserState(userId)
    activate SaveAPI
    SaveAPI-->>Server: 📊 서버 상태 반환
    deactivate SaveAPI
    Server-->>SyncMgr: 서버 상태 데이터
    deactivate Server

    SyncMgr->>SyncMgr: 🔍 충돌 감지 로직<br/>(로컬 vs 서버 timestamp 비교)
    
    alt 충돌 없음 (안전한 병합)
        SyncMgr->>Server: POST /api/users/{userId}/state/delta<br/>{ "deltas": [모든 오프라인 델타] }
        activate Server
        Server->>SaveAPI: MergeDeltasAsync(userId, deltas)
        SaveAPI-->>Server: ✅ 병합 성공
        Server-->>SyncMgr: HTTP 200 OK
        deactivate Server
        SyncMgr->>CacheMgr: ClearOfflineCache()
        
    else 충돌 발생 (중복 변경)
        SyncMgr->>SyncMgr: 🛠️ 충돌 해결 정책 적용<br/>(서버 우선 또는 클라이언트 우선)
        SyncMgr->>DataMgr: ResolveConflicts(serverState, localDeltas)
        DataMgr->>DataMgr: 🔄 데이터 정합성 복구
        DataMgr-->>Player: 🔔 "동기화 완료" 알림
    end
```

---

## **📋 아키텍처 흐름 특징**

### **데이터 동기화 패턴**
- `DataManager` 중심의 **Delta Event System**으로 실시간 동기화
- JSON 저장 → Delta 이벤트 발생 → 관련 Manager들에게 변경 알림
- UI 레이어는 Manager의 이벤트를 구독하여 자동 갱신

### **의존성 역전 원칙**
- `IItemUse` 인터페이스를 통한 **Strategy Pattern** 적용
- `ItemDatabase`가 ScriptableObject와 구현체를 연결
- 런타임에 동적으로 아이템 로직 실행

### **성능 최적화**
- `ItemDatabase` 캐싱으로 반복 에셋 로딩 방지
- 쿨다운 시스템으로 과도한 사용 방지
- Delta 이벤트 배치 처리로 I/O 최적화

### **네트워크 아키텍처 특징**
- **로컬 우선 처리**: 게임플레이 반응성을 위해 클라이언트에서 즉시 적용
- **배치 동기화**: 5초 간격으로 Delta 이벤트 일괄 업로드
- **충돌 해결**: 서버 검증 실패 시 정확한 상태로 강제 동기화
- **오프라인 지원**: 네트워크 단절 시 로컬 캐시, 복구 시 자동 병합
- **보안 검증**: 서버에서 비정상 패턴 감지 및 상태 보정
