# 로컬 테스트 환경 가이드 (VMware)

## 개요
실제 AWS 배포 전에 로컬 환경에서 가상머신을 사용하여 배포 과정을 테스트하는 방법을 설명합니다.

## 사전 준비사항
- VMware Workstation Pro (유료) 또는 VMware Player (무료)
- VirtualBox (무료 대안)
- 최소 8GB RAM, 50GB 디스크 여유 공간

## 1. 가상머신 소프트웨어 설치

### 1.1 VMware Workstation 설치
1. **VMware Workstation Pro** 다운로드
   - https://www.vmware.com/products/workstation-pro.html
   - 30일 무료 평가판 또는 라이센스 구매

2. **VMware Player (무료)** 대안
   - https://www.vmware.com/products/workstation-player.html
   - 개인 사용 무료

### 1.2 VirtualBox 설치 (무료 대안)
1. **VirtualBox** 다운로드
   - https://www.virtualbox.org/
   - 완전 무료 오픈소스

## 2. Linux 가상머신 생성

### 2.1 Ubuntu Server 20.04 LTS 다운로드
```
1. https://ubuntu.com/download/server 방문
2. Ubuntu Server 20.04.6 LTS ISO 다운로드
3. 약 1.4GB 크기
```

### 2.2 VMware에서 가상머신 생성
```
1. VMware 실행 → "Create a New Virtual Machine"
2. "Installer disc image file (iso)" 선택 → Ubuntu ISO 선택
3. VM 설정:
   - Name: JustClimb-Test-Server
   - RAM: 2GB (최소) ~ 4GB (권장)
   - Hard Disk: 20GB (Dynamic allocation)
   - Network: NAT (기본값)
4. Ubuntu 설치:
   - Username: testuser
   - Password: [설정할 비밀번호]
   - OpenSSH server 설치 체크
```

### 2.3 VirtualBox에서 가상머신 생성
```
1. VirtualBox 실행 → "새로 만들기"
2. VM 설정:
   - 이름: JustClimb-Test-Server
   - 종류: Linux
   - 버전: Ubuntu (64-bit)
   - 메모리: 2048MB
   - 하드 디스크: 동적 할당 20GB
3. 네트워크 설정:
   - 어댑터 1: NAT
   - 고급 → 포트 포워딩 규칙 추가
     - 호스트 포트: 2222 → 게스트 포트: 22 (SSH)
     - 호스트 포트: 8080 → 게스트 포트: 5000 (웹서버)
```

## 3. 테스트 환경 설정

### 3.1 SSH 접속 설정
```bash
# VMware (NAT 네트워크에서 VM IP 확인)
ip addr show

# VirtualBox (포트 포워딩 사용)
ssh testuser@localhost -p 2222

# VMware SSH 접속
ssh testuser@[VM_IP_ADDRESS]
```

### 3.2 필수 소프트웨어 설치
```bash
# 시스템 업데이트
sudo apt update && sudo apt upgrade -y

# .NET 9.0 설치
wget https://packages.microsoft.com/config/ubuntu/20.04/packages-microsoft-prod.deb -O packages-microsoft-prod.deb
sudo dpkg -i packages-microsoft-prod.deb
sudo apt update
sudo apt install -y dotnet-sdk-9.0

# PostgreSQL 설치 (로컬 테스트용)
sudo apt install -y postgresql postgresql-contrib

# Redis 설치 (로컬 테스트용)
sudo apt install -y redis-server

# Nginx 설치
sudo apt install -y nginx

# Git 설치
sudo apt install -y git
```

### 3.3 데이터베이스 설정
```bash
# PostgreSQL 설정
sudo -u postgres psql
postgres=# CREATE DATABASE just_climb_test;
postgres=# CREATE USER testuser WITH ENCRYPTED PASSWORD 'testpass';
postgres=# GRANT ALL PRIVILEGES ON DATABASE just_climb_test TO testuser;
postgres=# \q

# Redis 설정
sudo systemctl start redis-server
sudo systemctl enable redis-server
redis-cli ping  # PONG 응답 확인
```

## 4. 서버 배포 테스트

### 4.1 프로젝트 클론 및 설정
```bash
# 프로젝트 클론
git clone https://github.com/[YOUR_USERNAME]/Just_Climb.git
cd Just_Climb/Server/Server

# 테스트용 설정 파일 생성
cp appsettings.json appsettings.Test.json
```

### 4.2 appsettings.Test.json 설정
```json
{
  "ConnectionStrings": {
    "DefaultConnection": "Host=localhost;Database=just_climb_test;Username=testuser;Password=testpass",
    "Redis": "localhost:6379"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Redis": {
    "Configuration": "localhost:6379",
    "InstanceName": "JustClimbTest"
  }
}
```

### 4.3 서버 빌드 및 실행
```bash
# 데이터베이스 마이그레이션
dotnet ef database update --environment Test

# 서버 빌드 및 실행
dotnet build
dotnet run --environment Test
```

### 4.4 테스트 실행
```bash
# 새 터미널에서 API 테스트
curl http://localhost:5000/health
curl http://localhost:5000/api/ranking
curl http://localhost:5000/api/achievements
```

## 5. 배포 스크립트 테스트

### 5.1 배포 스크립트 생성
```bash
#!/bin/bash
# deploy-test.sh

echo "=== Just Climb 테스트 배포 시작 ==="

# 1. 서버 중지
echo "서버 중지 중..."
pkill -f "dotnet.*Server"

# 2. 최신 코드 업데이트
echo "코드 업데이트 중..."
git pull origin main

# 3. 빌드
echo "빌드 중..."
dotnet publish -c Release -o ./publish

# 4. 배포
echo "배포 중..."
sudo cp -r ./publish/* /var/www/justclimb/

# 5. 서버 시작
echo "서버 시작 중..."
cd /var/www/justclimb
nohup dotnet Server.dll --environment Test > server.log 2>&1 &

echo "=== 배포 완료 ==="
```

### 5.2 스크립트 실행 권한 및 테스트
```bash
chmod +x deploy-test.sh
./deploy-test.sh
```

## 6. Docker를 이용한 컨테이너 테스트

### 6.1 Docker 설치
```bash
# Docker 설치
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh
sudo usermod -aG docker $USER
```

### 6.2 Dockerfile 생성
```dockerfile
FROM mcr.microsoft.com/dotnet/aspnet:9.0
WORKDIR /app
COPY ./publish .
EXPOSE 5000
ENTRYPOINT ["dotnet", "Server.dll"]
```

### 6.3 Docker Compose 설정
```yaml
# docker-compose.test.yml
version: '3.8'
services:
  app:
    build: .
    ports:
      - "5000:5000"
    environment:
      - ASPNETCORE_ENVIRONMENT=Test
    depends_on:
      - postgres
      - redis
  
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: just_climb_test
      POSTGRES_USER: testuser
      POSTGRES_PASSWORD: testpass
    ports:
      - "5432:5432"
  
  redis:
    image: redis:7
    ports:
      - "6379:6379"
```

### 6.4 Docker 컨테이너 테스트
```bash
# 컨테이너 빌드 및 실행
docker-compose -f docker-compose.test.yml up --build

# 테스트
curl http://localhost:5000/health
```

## 7. 성능 및 부하 테스트

### 7.1 Apache Bench 설치 및 테스트
```bash
# Apache Bench 설치
sudo apt install -y apache2-utils

# 간단한 부하 테스트
ab -n 1000 -c 10 http://localhost:5000/api/ranking
```

### 7.2 사용자 시뮬레이션 테스트
```bash
# 다중 사용자 API 호출 스크립트
#!/bin/bash
for i in {1..10}; do
  curl -X POST http://localhost:5000/api/ranking \
    -H "Content-Type: application/json" \
    -d '{"userId":"test'$i'","score":'$((RANDOM % 10000))'}' &
done
wait
```

## 8. 모니터링 도구 설정

### 8.1 htop 설치 (시스템 모니터링)
```bash
sudo apt install -y htop
htop  # 실행
```

### 8.2 로그 모니터링
```bash
# 실시간 로그 확인
tail -f server.log

# systemd 로그 확인 (서비스로 등록한 경우)
sudo journalctl -u justclimb -f
```

## 완료 체크리스트
- [ ] VMware/VirtualBox 설치 및 Ubuntu VM 생성
- [ ] SSH 접속 설정
- [ ] PostgreSQL 및 Redis 로컬 설치
- [ ] .NET 9.0 개발 환경 구축
- [ ] 서버 애플리케이션 빌드 및 실행 테스트
- [ ] API 엔드포인트 기능 테스트
- [ ] 배포 스크립트 테스트
- [ ] Docker 컨테이너 테스트 (선택사항)
- [ ] 간단한 부하 테스트
- [ ] 모니터링 도구 설정

## 다음 단계
로컬 테스트가 성공하면:
1. AWS 실제 환경에서 동일한 과정 수행
2. 도메인 설정 및 SSL 인증서 적용
3. 프로덕션 환경 모니터링 설정
4. 백업 및 복구 시스템 구축

---
**💡 팁**: 가상머신에서 스냅샷 기능을 활용하여 테스트 전 상태를 저장해두면 언제든 되돌릴 수 있습니다. 